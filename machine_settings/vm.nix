{ config, pkgs, lib, ... }:

{
  # --- MODULES ---
  imports = [
    # lister les imports
  ];

# ajouter les packets qui permettent la bonne communication avec le système hote (spice webdav?)

  # --- TUNING ---

}
