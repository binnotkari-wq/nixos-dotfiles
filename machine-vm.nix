{ config, pkgs, lib, ... }:

{
  # --- MODULES ---
  imports = [
  #./modules/tests.nix
  # ./modules/OS-optimizations_stateless.nix # commenter si on utilise impermanence
  ];

  # --- TUNING ---

}
